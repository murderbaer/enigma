#include "enigma.h"
#include <stdlib.h>

Enigma *createM3Enigma(Rotor *rotor_one, Rotor *rotor_two, Rotor *rotor_three,
                       Reflector *reflector, Plugboard *plugboard)
{
    return malloc(sizeof(Enigma));
}

Enigma *createM4Enigma(Rotor *rotor_one, Rotor *rotor_two, Rotor *rotor_three,
                       Rotor *rotor_four, Reflector *reflector,
                       Plugboard *plugboard)
{
    return malloc(sizeof(Enigma));
}